
// This component is no longer needed as a custom page transition loader is implemented.
// Returning null effectively disables the default Next.js loading UI for route groups.
export default function Loading() {
  return null;
}
